Welcome curious user! You've found a hidden README file!

The following properties can be added or change in orbit.properties:

# interface
# values = (controller|manager)
## controller (default) -> simple vm controller
## manager -> replacement for vsphere client
interface=controller
# interface.close
# values = (true|false)
## quit application when a window is closed
interface.close=true
# interface.login.advance
# values = (true|false)
## show advance login
interface.login.advance=true
# recent user/server - auto updated on successful login
recent.login=root
recent.server=apollo.vsphere.example.org
recent.tunnel.login=user
recent.tunnel.server=shell.srv.example.org